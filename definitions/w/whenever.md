# whenever

whenever; **when ever**. The usual expression is the solid *whenever* (= \[1\] at any time; or \[2\] every time that a specified thing happens). The two-word version is used as an emphatic extension of *when* in a question <when ever did you say such a thing?>.

